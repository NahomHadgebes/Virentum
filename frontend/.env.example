# Base URL of the Virentum API. No trailing slash.
# The value must be listed in the backend's Cors:AllowedOrigins-compatible
# configuration; the dev API listens on http://localhost:5000.
# There is no fallback: the app fails at startup if this is missing.
VITE_API_BASE_URL=http://localhost:5000
